name: nd_core_utils
description: An utility library for dart.
version: 1.0.21
homepage: https://github.com/hiep-nd/flutter_nd_core_utils

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  tuple: ^2.0.0
  logging: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
